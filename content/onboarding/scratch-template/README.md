# greet

A tiny command-line tool that says hello.

    python3 greet.py World
    Hello, World!

Scratch project for the AIDA onboarding tour (`aida-tutor onboard`). You'll
add one small feature to it — a `--upper` flag — and watch AIDA record how
that feature came to be.
